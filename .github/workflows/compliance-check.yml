name: Compliance Check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  compliance:
    uses: CHITTYOS/chittyops/.github/workflows/reusable-compliance-check.yml@main
    with:
      service_name: 'chittybeacon'
      tier: 3
      check_health: true
      domain: 'beacon.chitty.cc'
